# Server Configuration
PORT=3001
NODE_ENV=development

# Firebase Configuration
# Get these values from your Firebase Console > Project Settings > Service Accounts
# Download the service account JSON and extract these fields
FIREBASE_PROJECT_ID=<your-project-id>
FIREBASE_PRIVATE_KEY=<from-service-account-json>
FIREBASE_CLIENT_EMAIL=<from-service-account-json>

# SurrealDB Configuration
# For production: Use SurrealDB Cloud or self-hosted instance
# For development: Use local instance (surreal start --user root --pass root)
SURREALDB_URL=ws://localhost:8000
SURREALDB_USER=root
SURREALDB_PASS=root
SURREALDB_NS=couples_therapy
SURREALDB_DB=main

# OpenAI Configuration
# Required for GPT-5.2 chat completions and text-embedding-3-large embeddings
# Used for: AI conversations, guidance synthesis, relationship orchestration, and vector similarity search
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=<your-openai-api-key>

# Redis Configuration
# For production: Use Redis Cloud or managed Redis service
# For development: Use local Redis (docker run -p 6379:6379 redis)
REDIS_URL=redis://localhost:6379

# Anthropic Configuration (Optional)
# Required if using Claude for guidance synthesis
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=<your-anthropic-api-key>
